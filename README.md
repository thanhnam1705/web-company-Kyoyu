
Only use npm command

  * ### Installation
      ```
      npm install -g grunt-cli
      ```
      ```
      npm install
      ```

  * ### Build and watch for development
      ```
      grunt
      ```